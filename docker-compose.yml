version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: lawfirm-app
    restart: always
    ports:
      - "3011:3000"

    environment:
      DATABASE_URL: postgresql://rizaldi:15031996@postgres:5432/lawfirm_db
      NEXTAUTH_URL: https://lawfirm.zelixa.my.id
      NEXTAUTH_URL_INTERNAL: http://localhost:3000
      NEXTAUTH_SECRET: co-lawfirm-key-2026-v1
      NEXTAUTH_TRUST_HOST: "true"
      MINIO_ENDPOINT: storage.zelixa.my.id
      MINIO_PORT: 80
      MINIO_ACCESS_KEY: rizaldi
      MINIO_SECRET_KEY: 15031996
      MINIO_BUCKET: lawfirm
      MINIO_USE_SSL: "false"

      NEXT_PUBLIC_MINIO_URL: http://storage.zelixa.my.id

    networks:
      - global-network

networks:
  global-network:
    external: true
